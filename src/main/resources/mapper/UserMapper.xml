<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxg.blog.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.xxg.blog.entity.User">
            <id property="userId" column="user_id" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="userNickname" column="user_nickname" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="email" column="email" jdbcType="VARCHAR"/>
            <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="birthday" column="birthday" jdbcType="DATE"/>
            <result property="age" column="age" jdbcType="INTEGER"/>
            <result property="mobilePhone" column="mobile_phone" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id,username,user_nickname,
        password,email,avatar,
        create_time,birthday,age,
        mobile_phone
    </sql>
    <select id="loadUserByUsername" resultType="com.xxg.blog.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <select id="getRolesByUid" resultType="com.xxg.blog.entity.Role">
        SELECT * FROM role r, user_role ur WHERE r.id = ur.rid AND ur.uid = #{uid}
    </select>
</mapper>
